.First <- function() {
  options(repos = c(CRAN = 'https://cran.rstudio.com/'),
          browserNLdisabled = TRUE,
          deparse.max.lines = 2)
  .libPaths('~/.R/library')
}

if (interactive()) {
  suppressMessages(require(dplyr))
}
